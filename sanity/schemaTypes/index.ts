import { about } from "./about";
import { localizedText, localizedTextBlock } from "./localizedText";
import { project } from "./project";
import { siteSettings } from "./siteSettings";

export const schemaTypes = [localizedText, localizedTextBlock, project, siteSettings, about];

